version: '3'

services:
  zk-circuit-tester:
    build:
      context: ../..
      dockerfile: lib/zk/Dockerfile
    volumes:
      - ../../lib/zk:/app/lib/zk
    command: bash
    environment:
      - NODE_ENV=development